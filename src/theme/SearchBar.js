import React from 'react';
import { LearnHeader } from '@ebook/core';

export default function SearchBarWrapper(props) {

    return (
        <div id='learnHeader'>
            <LearnHeader />
        </div>
    );
}